# my-express-server

Simple Express app that listens on port 8001. Uses `nodemon` for automatic code reloading during development.

## Running Locally

Install dependencies and start the dev server:

```bash
npm install
npm run dev
```

## Running with Docker

Build and run with Docker Compose from the repository root:

```bash
docker compose up --build
```